[project]
name = "etw-analysis"
version = "0.1.0"
description = "Cluster-aware re-analysis of the ablation pilot, fair baseline and deployment study"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.26",
    "pandas>=3.0.3",
    "scipy>=1.11",
    "statsmodels>=0.14",
]
